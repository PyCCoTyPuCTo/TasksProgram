<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
</head>

<body>
    <div class=" container">
        @Html.ActionLink("Главная", "Index", "Home", new { @class = "btn btn-default" })
        @Html.ActionLink("Создать заметку", "AddNew", "Home", new { @class = "btn btn-default" })
    </div>
    <div class="container">
        @RenderBody()
    </div>

</body>
</html>
